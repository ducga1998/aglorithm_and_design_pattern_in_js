function MergeSort(arr = []){
    const midd =  arr.length
    const left = arr.slice( 0 , midd)
    const right =  arr.slice(midd)

}
const Merge = (left , right) => {
    // if()
    return Merge()
}
